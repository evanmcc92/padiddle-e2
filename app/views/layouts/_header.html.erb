<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<%= link_to "Home", root_path, class: "navbar-brand mb-0 h1" %>
	<ul class="nav">
		<% User.all.each do |u| %>
			<li class="nav-item <%= 'active' if current_page?(user_path(u.id)) %>">
				<%= link_to u.name, user_path(u.id), class: "nav-link" %>
			</li>
		<% end %>
		<li class="nav-item <%= 'active' if current_page?(padiddle_points_path) %>">
			<%= link_to 'All Points', padiddle_points_path, class: "nav-link" %>
		</li>
	</ul>
</nav>